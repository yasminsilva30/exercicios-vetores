#include<stdio.h>

main(){

    int vetor[6], x[1], y[1], soma;

    for (int i = 0; i < 6; i++) {
        printf("Digite o numero: \n");
        scanf("%d", &vetor[i]);
    }

    for (int i = 0; i < 6; i++) {
        printf("\n Numero inserido: %d", vetor[i]);
        printf("\n Valor da soma entre X e Y: %d", soma);
    }

}